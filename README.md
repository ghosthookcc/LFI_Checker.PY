# LFI_Checker.PY
A program that uses LFI vulnerabilities to exploit a LFI vulnerable website, I will also try to make a crawler for the program so it also can find vulnerabilites on the website. This is so it doesn't only automate something as simple as a LFI Attack.
